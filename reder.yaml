services:
  - type: web
    name: test-drive-api
    env: python
    buildCommand: ""
    startCommand: streamlit run app.py
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: testdrive-db
          property: connectionString
