services:
  - type: web
    name: test-drive-api
    env: python
    buildCommand: ""
    startCommand: python app.py
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: testdrive-db
          property: connectionString
